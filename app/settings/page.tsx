"use client";

import { Heading, Stack, Text } from "@chakra-ui/react";

export default function SettingsPage() {
  return (
    <Stack gap="2" padding="6">
      <Heading size="lg">Settings</Heading>
      <Text color="fg.muted">Placeholder — not built yet.</Text>
    </Stack>
  );
}
